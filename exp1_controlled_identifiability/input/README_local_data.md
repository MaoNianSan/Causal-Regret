# Local Data

EXP1 is a controlled synthetic simulation and does not require third-party raw data.

Do not place generated outputs in this directory. Runtime artifacts belong under `outputs/<mode>/`.
