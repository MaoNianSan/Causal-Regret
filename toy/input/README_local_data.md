# Local Data

Toy uses a synthetic, version-controlled experiment design and does not require third-party raw data.

Do not place generated outputs in this directory. Runtime artifacts belong under `outputs/<mode>/`.
